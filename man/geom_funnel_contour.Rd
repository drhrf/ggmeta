% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-funnel.R
\name{geom_funnel_contour}
\alias{geom_funnel_contour}
\title{Pseudo-confidence-interval contours for a funnel plot}
\usage{
geom_funnel_contour(
  centre,
  se_max,
  level = 0.95,
  ...,
  colour = "grey55",
  linetype = "dashed",
  linewidth = 0.4,
  alpha = NA
)
}
\arguments{
\item{centre}{Reference effect (usually the pooled estimate) the funnel is
centred on, on the analysis scale.}

\item{se_max}{Largest standard error the contours should reach (the bottom of
the funnel).}

\item{level}{Confidence level(s) for the contour(s), e.g. \code{0.95} or
\code{c(0.95, 0.99)}. Default \code{0.95}.}

\item{...}{Other arguments passed on to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{colour, linetype, linewidth, alpha}{Line appearance.}
}
\value{
A ggplot2 layer.
}
\description{
\code{geom_funnel_contour()} draws the funnel-shaped pseudo confidence interval(s)
around a reference effect. For a study with standard error \code{se}, the expected
effect under no bias lies within \code{centre} +/- z * \code{se}, so the contours are
straight lines fanning out from the apex at \code{se = 0}. On a funnel plot with a
reversed standard-error axis they form the familiar funnel.
}
\details{
The layer builds its own data from \code{centre}, \code{se_max}, and \code{level}, so it does
not use the plot's data; add it to a plot that maps the effect to \code{x} and the
standard error to \code{y}.
}
\examples{
library(ggplot2)
studies <- data.frame(
  estimate = c(-0.5, -0.2, -0.4, 0.1, -0.3),
  se       = c(0.10, 0.30, 0.15, 0.35, 0.22)
)
ggplot(studies, aes(x = estimate, y = se)) +
  geom_funnel_contour(centre = -0.3, se_max = 0.4) +
  geom_point() +
  scale_y_reverse()
}
