% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy-meta.R
\name{tidy_meta}
\alias{tidy_meta}
\alias{tidy_meta.default}
\alias{tidy_meta.data.frame}
\alias{tidy_meta.meta}
\title{Tidy a 'meta' object into a plottable data frame}
\usage{
tidy_meta(x, ...)

\method{tidy_meta}{default}(x, ...)

\method{tidy_meta}{data.frame}(
  x,
  add_summary = FALSE,
  summary_method = c("common", "random"),
  level = 0.95,
  ...
)

\method{tidy_meta}{meta}(
  x,
  back_trans = c("auto", "exp", "none"),
  sort_studies = TRUE,
  add_summary = TRUE,
  add_predict = TRUE,
  add_subgroups = TRUE,
  ...
)
}
\arguments{
\item{x}{An object of class \code{meta}, e.g. created by
\code{\link[meta:metabin]{meta::metabin()}}, \code{\link[meta:metacont]{meta::metacont()}}, or \code{\link[meta:metagen]{meta::metagen()}}.}

\item{...}{Additional arguments passed to methods.}

\item{add_summary}{For the data-frame method, if \code{TRUE} compute an
inverse-variance / DerSimonian-Laird pooled summary from the study rows
and append it (on-the-fly meta-analysis, no \pkg{meta} package required).
Needs a \code{se} column, or \code{ci_lower}/\code{ci_upper} to recover it. Default
\code{FALSE}.}

\item{summary_method}{Which pooled summaries to add when \code{add_summary = TRUE}: \code{"common"}, \code{"random"}, or both (default).}

\item{level}{Confidence level for the pooled summary interval. Default
\code{0.95}.}

\item{back_trans}{Should the estimate and confidence limits be
back-transformed to the natural scale? If \code{"auto"} (default), each summary
measure is back-transformed with its correct inverse via
\code{\link[meta:backtransf]{meta::backtransf()}} (exponentiation for ratios, inverse-logit for
\code{PLOGIT}, Fisher's z to correlation for \code{ZCOR}, etc.); linear measures are
left unchanged. Use \code{"exp"} to force exponentiation or \code{"none"} to keep
the analysis scale.}

\item{sort_studies}{If \code{TRUE} (default), sort studies by effect estimate
(most favorable at top).}

\item{add_predict}{If \code{TRUE} (default), include prediction interval row
when available.}

\item{add_subgroups}{If \code{TRUE} (default), include subgroup headers and
within-group summary rows when subgroups are present.}
}
\value{
A \code{data.frame} with one row per study or summary, with columns:
\describe{
\item{studlab}{Study label (character)}
\item{estimate}{Point estimate (numeric)}
\item{ci_lower}{Lower confidence limit (numeric)}
\item{ci_upper}{Upper confidence limit (numeric)}
\item{se}{Standard error (numeric)}
\item{weight}{Study weight (numeric, \code{NA} for summaries)}
\item{p_value}{P-value (numeric)}
\item{n}{Sample size or person-time (numeric, optional)}
\item{event}{Number of events (numeric, optional)}
\item{is_summary}{Logical, \code{TRUE} for summary rows}
\item{summary_type}{Character: \code{"none"}, \code{"common"},
\code{"random"}, \code{"subgroup"}, or \code{"predict"}}
\item{subgroup}{Subgroup label (character or \code{NA})}
}

The returned data frame has the following attributes:
\describe{
\item{\code{sm}}{summary measure type (e.g. \code{"RR"}, \code{"OR"}, \code{"MD"})}
\item{\code{null_effect}}{null effect value for reference line}
\item{\code{method}}{meta-analysis method}
\item{\code{common}}{logical, TRUE if common effect model was used}
\item{\code{random}}{logical, TRUE if random effects model was used}
\item{\code{tau}}{heterogeneity estimate tau}
\item{\code{k}}{number of studies}
}
}
\description{
Converts objects of class \code{meta} (from the \pkg{meta} package) into
a tidy data frame suitable for use with ggmeta geometries and
\code{\link[=ggforest]{ggforest()}}. The returned data frame has one row per study or summary.
}
\examples{
\donttest{
library(meta)
m <- metabin(event.e, n.e, event.c, n.c,
  data = data.frame(
    event.e = c(14, 30), n.e = c(100, 150),
    event.c = c(10, 25), n.c = c(100, 150)
  ),
  studlab = c("Study A", "Study B"),
  sm = "RR"
)
tidy_meta(m)
}
}
