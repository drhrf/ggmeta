% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggforest.R
\name{ggforest}
\alias{ggforest}
\alias{ggforest.default}
\alias{ggforest.meta}
\alias{ggforest.data.frame}
\title{Create a forest plot}
\usage{
ggforest(x, ...)

\method{ggforest}{default}(x, ...)

\method{ggforest}{meta}(
  x,
  ...,
  back_trans = c("auto", "exp", "none"),
  sort_studies = TRUE,
  show_summary = TRUE,
  show_predict = TRUE,
  show_hetstats = TRUE,
  null_effect = NULL,
  xlab = NULL
)

\method{ggforest}{data.frame}(
  x,
  null_effect = 0,
  xlab = "Effect (95\% CI)",
  ylab = NULL,
  title = NULL,
  caption = NULL,
  add_summary = FALSE,
  summary_method = c("common", "random"),
  level = 0.95,
  columns = NULL,
  effect_header = NULL,
  ci_args = list(),
  diamond_args = list(),
  diamond_colours = NULL,
  predict_args = list(),
  ref_args = list(),
  consensus = TRUE,
  consensus_args = list(),
  ...
)
}
\arguments{
\item{x}{A \pkg{meta} object or a tidy data frame with columns
\code{estimate}, \code{ci_lower}, \code{ci_upper}, \code{studlab}, and optionally
\code{weight}, \code{is_summary}, \code{summary_type}, \code{subgroup}.}

\item{...}{Additional arguments passed to methods.}

\item{back_trans}{Back-transform ratio measures? \code{"auto"} (default),
\code{"exp"}, or \code{"none"}.}

\item{sort_studies}{Sort studies by effect estimate? Default: \code{TRUE}.}

\item{show_summary}{Include summary effect rows? Default: \code{TRUE}.}

\item{show_predict}{Include prediction interval? Default: \code{TRUE}
(only when random effects model is present).}

\item{show_hetstats}{Show heterogeneity statistics in plot caption?
Default: \code{TRUE}.}

\item{null_effect}{Null effect value for the reference line.
Default: \code{0}.}

\item{xlab}{X-axis label. Default: \code{"Effect (95\% CI)"}.}

\item{ylab}{Y-axis label. Default: \code{NULL} (no label — study labels
serve as the y-axis text).}

\item{title}{Plot title. Default: \code{NULL}.}

\item{caption}{Plot caption. Default: \code{NULL}.}

\item{add_summary}{For the data-frame method, if \code{TRUE} compute a pooled
summary from the study rows (inverse-variance and/or DerSimonian-Laird)
and draw it as a diamond — on-the-fly meta-analysis without the
\pkg{meta} package. Needs a \code{se} column, or \code{ci_lower}/\code{ci_upper} to
recover it. Default: \code{FALSE}.}

\item{summary_method}{Which pooled summaries to add when \code{add_summary = TRUE}: \code{"common"}, \code{"random"}, or both (default).}

\item{level}{Confidence level for the pooled summary interval. Default
\code{0.95}.}

\item{columns}{Add a \code{meta::forest()}-style table of text columns to the
right of the plot. \code{TRUE} shows the effect estimate, 95\% CI, and weight;
or pass a subset/order such as \code{c("estimate", "ci")}. \code{NULL} (default)
draws no columns.}

\item{effect_header}{Header for the estimate column (e.g. \code{"Hedges' g"}).
Defaults to the summary measure (e.g. \code{"SMD"}, \code{"RR"}).}

\item{ci_args, diamond_args, predict_args}{Lists of arguments used to restyle
the study confidence intervals (\code{\link[=geom_forest_ci]{geom_forest_ci()}}), the summary diamonds
(\code{\link[=geom_forest_diamond]{geom_forest_diamond()}}), and the prediction interval
(\code{\link[=geom_forest_predict]{geom_forest_predict()}}). For example
\code{predict_args = list(cap_width = 0.1, colour = "red")} or
\code{ci_args = list(colour = "grey20", point_size_range = c(1, 5))}. This is the
way to customise these elements: adding another \verb{geom_forest_*()} layer to a
\code{ggforest()} plot draws a \emph{second} layer over every row rather than
restyling the built-in one.}

\item{diamond_colours}{Optional named colours for the summary diamonds,
overriding the default palette. Names are \code{"common"}, \code{"random"},
\code{"subgroup_common"}, and \code{"subgroup_random"}, e.g.
\code{c(common = "black", random = "steelblue")}.}

\item{ref_args, consensus_args}{Lists of arguments for the null-effect
reference line and the dotted "consensus" line (both \code{\link[=geom_forest_ref]{geom_forest_ref()}}),
e.g. \code{ref_args = list(linetype = "dashed", colour = "black")}.}

\item{consensus}{Draw the dotted consensus line at the pooled estimate?
Default \code{TRUE} (only shown alongside a null line).}
}
\value{
A \code{ggplot} object. Add standard ggplot2 layers (themes,
scales, labels) to further customize.
}
\description{
The primary function for creating publication-quality forest plots.
Accepts a \pkg{meta} object (created by \code{\link[meta:metabin]{meta::metabin()}},
\code{\link[meta:metacont]{meta::metacont()}}, etc.) or a tidy data frame (as returned by
\code{\link[=tidy_meta]{tidy_meta()}} or constructed manually).
}
\examples{
\donttest{
library(meta)
m <- metabin(event.e, n.e, event.c, n.c,
  data = data.frame(
    event.e = c(14, 30, 15, 22),
    n.e     = c(100, 150, 100, 120),
    event.c = c(10, 25, 12, 18),
    n.c     = c(100, 150, 100, 120)
  ),
  studlab = c("Study A", "Study B", "Study C", "Study D"),
  sm = "RR"
)
ggforest(m)
}
}
