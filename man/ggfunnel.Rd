% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggfunnel.R
\name{ggfunnel}
\alias{ggfunnel}
\alias{ggfunnel.default}
\alias{ggfunnel.meta}
\alias{ggfunnel.data.frame}
\title{Funnel plot from a meta-analysis}
\usage{
ggfunnel(x, ...)

\method{ggfunnel}{default}(x, ...)

\method{ggfunnel}{meta}(x, ..., ref = c("common", "random"), level = 0.95)

\method{ggfunnel}{data.frame}(
  x,
  centre = NULL,
  sm = NULL,
  level = 0.95,
  xlab = NULL,
  ylab = "Standard error",
  title = NULL,
  ...
)
}
\arguments{
\item{x}{A \pkg{meta} object, or a data frame with \code{estimate} and \code{se}
columns (study effects and standard errors on the analysis scale).}

\item{...}{Additional arguments passed to methods.}

\item{ref}{Which pooled estimate to centre the funnel on: \code{"common"} (fixed
effect, default) or \code{"random"}.}

\item{level}{Confidence level(s) for the funnel contours, e.g. \code{0.95} or
\code{c(0.95, 0.99)}. Default \code{0.95}.}

\item{centre}{Effect the funnel is centred on. Defaults to the
inverse-variance (common-effect) estimate of the supplied studies.}

\item{sm}{Summary measure (e.g. \code{"RR"}), used to label the x-axis and, for
ratio measures, to show back-transformed axis labels. Optional.}

\item{xlab, ylab}{Axis labels. \code{ylab} defaults to \code{"Standard error"}.}

\item{title}{Plot title. Default \code{NULL}.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{ggfunnel()} draws a funnel plot: each study's effect estimate against its
standard error (a measure of precision), with pseudo confidence-interval
contours around the pooled effect. Asymmetry can signal small-study effects
or publication bias. Like \code{\link[=ggforest]{ggforest()}}, it accepts a \pkg{meta} object or a
tidy data frame and returns an ordinary \code{ggplot}, so a forest and a funnel
plot compose on one canvas with, for example, \pkg{patchwork}.
}
\examples{
\donttest{
library(meta)
m <- metabin(event.e, n.e, event.c, n.c,
  data = data.frame(
    event.e = c(12, 8, 25, 18, 30, 15), n.e = c(120, 90, 200, 150, 250, 130),
    event.c = c(20, 14, 30, 28, 35, 25), n.c = c(118, 92, 205, 148, 245, 128)
  ),
  studlab = paste("Study", 1:6), sm = "RR"
)
ggfunnel(m)
}
}
